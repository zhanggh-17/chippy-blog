<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.chippy.blog.mapper.TagMapper">
    <select id="list" resultType="top.chippy.blog.entity.Tag">
        SELECT id, `name` FROM chippy_tag
    </select>
</mapper>